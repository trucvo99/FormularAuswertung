<!---------------- Truc Vo ---------------->
<!--------- Matrikelnummer: 2575005 --------->

<!DOCTYPE html>
<html>
    <head>
        <title>Login Information</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">  
        <style>
            header {
                text-align: center;
                color: plum;
                font-family: sans-serif;
                font-size: 30px;
                font-weight: bolder;
            }

            h1 {
                font-weight: bolder;
                font-family: sans-serif;
                font-size: 20px;
                text-align:initial;
                color: purple;
            }

            h2 {
                font-weight: bolder;
                font-family: sans-serif;
                font-size: 15px;
                color: purple;
            }

            p {
                font-family: sans-serif;
            }

            a {
                font-family: sans-serif;
                font-weight: bolder;
                color: purple;
            }

            i {
                color: purple;
            }
        </style>
    </head>
    <body>
        <header>LOGIN-DATEN</header>

        <!-- prüfen, ob ein Benutzer in dieser Tabelle steht -->    
        <% for (info of benutzerInfo){ %>
            <% if (info.benutzername == name) { %>
                <h1>Herzlich Willkommen Zurück, <%= name %>!</h1>
                <p>Schön, dass Sie wieder hier sind. </p>
                <% break %>
            <% } %> 
        <% } %>
        <% if (info.benutzername != name){ %>
            <p>
                Der Benutzername ist nicht vorhanden. Möchten Sie registrieren?
                <i class="material-icons">arrow_forward</i>
                <a href="/registrieren">Zur Registrierseite</a>
            </p>
        <% } %>
        <!-- input: Carla, output: Herzlich Willkommen -->
        <!-- input: Nana, output: nicht vorhanden -->
        

        <!-- prüfen, ob da zu einem Benutzer passende Passwort eingegeben wurde -->
        <% for (info of benutzerInfo){ %>
            <% if (info.benutzername == name && info.passwort == pass){ %>
                <h2>LOGIN ERFOLGREICH</h2>
                <p> Der Benutzername und das Passwort sind übereinstimmt. </p>
                <p> 
                    Möchten Sie abmelden?
                    <i class="material-icons">arrow_forward</i>
                    <a href="/login">Logout</a>
                </p>
                <% break %>
            <% } %>
        <% } %>
        <% if (info.benutzername == name && info.passwort != pass){ %>
            <h2>LOGIN-FEHLER</h2>
            <p>Bitte prüfen Sie Ihre Login-Daten noch einmal!</p>
        <% } %>
        <!-- input: Carla (Benutzername), 12333333 (Passwort), output: LOGIN ERFOLGREICH -->
        <!-- input: Carla (Benutzername), 123 (Passwort), output: LOGIN-Fehler -->

        <br><br>
        <p>
            <a href="/login">Zurück zur Login-Seite</a><i class="material-icons">exit_to_app</i> |
            <a href="/registrieren">Zur Registrierseite</a><i class="material-icons">open_in_new</i>
        </p>
    </body>
    
</html>